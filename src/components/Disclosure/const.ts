import { IRedWhiteTitle } from "../../types/interfaces/RedWhiteTitle.ts";

export const TITLE_DATA: IRedWhiteTitle = {
    FIRST: 'financial',
    LAST: 'disclosure'
}

export const WINDOW_TITLE_DATA: IRedWhiteTitle = {
    FIRST: 'There is',
    LAST: 'No \nExpectation of Profit'
}

export const WINDOW_DESCRIPTION: string = 'You should not expect to profit from purchasing and holding KOMPETE Token. KOMPETE Token is a utility token within the KOMPETE game economy. KOMPETE Token is not an investment or security, so don’t view it as such. KOMPETE Token is a virtual currency for the KOMPETE game economy and nothing more than that.'